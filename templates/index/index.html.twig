{% extends 'base.html.twig' %}

{% block body %}
    <div class="container-fluid">
        <div class="row">
            <div class="col-6" style="background-color: #0000FF; color: white; min-height: 25px">
                Hello World: {{ msg }}
            </div>
            <div class="col-6">
                aoeu
                {{ dump(contacts) }}
                {% for key, contact in contacts %}
                    key: {{ key }}
                   {{ contact.email }}
                    <br>
                {% endfor %}
            </div>
        </div>
    </div>
    {#{{ parent() }}#}
{% endblock %}